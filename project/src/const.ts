export enum AppRoute {
  Login = '/login',
  Root = '/',
  Favorites = '/favorites',
  Offer = '/offer',
}

export enum AuthorizationStatus {
  Authorize = 'authorize',
  NotAuthorize = 'not-authorize',
  Unknown = 'unknown',
}
